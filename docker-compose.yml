version: "3.3"

services:
  app:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    restart: always
    working_dir: /home/laravel/site
    tty: false
    volumes:
      - ./src:/home/laravel/site
    networks:
      - laravel

  mysql:
    image: mysql:5.7.29
    restart: always
    networks:
      - laravel

  nginx:
    image: nginx:stable-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./src:/home/laravel/site
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - laravel
      
networks:
  laravel:
    driver: bridge

volumes:
  phpmyadmin:
    driver: local
  mysql:
    driver: local